{
  "name": "@xsai-chromium-prompt/root",
  "type": "module",
  "version": "0.2.0",
  "private": true,
  "packageManager": "pnpm@11.8.0",
  "description": "Chromium Prompt API wrapper makes it easy to use with xsai",
  "repository": "github:moeru-ai/xsai-chromium-prompt",
  "scripts": {
    "build": "pnpm run package:build && pnpm -r -F=\"./playground\" run build",
    "dev": "pnpm run package:build && pnpm -r -F=\"./playground\" run dev",
    "package:build": "pnpm -r -F=\"./package\" run build",
    "publish": "pnpm run package:build && cp ./README.md ./package/README.md && pnpm -r -F=\"./package\" publish && rm ./package/README.md"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^6.2.0",
    "@importantimport/eslint-config": "^1.0.0-beta.2",
    "@unocss/eslint-config": "^66.5.6",
    "@unocss/eslint-plugin": "^66.5.6",
    "eslint": "^9.39.1",
    "lint-staged": "^16.2.6",
    "pkgroll": "^2.27.1",
    "typescript": "~5.9.3"
  },
  "workspaces": [
    "package",
    "playground"
  ],
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "^11.5.3",
      "onFail": "download"
    }
  },
  "simple-git-hooks": {
    "pre-commit": "pnpm lint-staged"
  },
  "lint-staged": {
    "*": "eslint --fix"
  }
}
